import template from './home.template.html';
import './index.less';

class Controller {
    constructor($scope) {
    }
}

export default {
    template, 
    controller: Controller,
    $name: 'ttHome'
}