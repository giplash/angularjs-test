import template from './index.template.html';
import './index.less';

class Controller {
    constructor($scope) {
    }
}

export default {
    template, 
    controller: Controller,
    $name: 'ttHome'
}