import template from'./column.template.html';

class Controller {
    constructor() {
    }
}

export default {
    template,
    controller: Controller,
    $name: 'ttColumn',
    bindings: {     
    }
}